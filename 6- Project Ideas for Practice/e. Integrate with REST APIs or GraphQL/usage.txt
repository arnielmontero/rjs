Here’s a project idea for practice:

Integrate with REST APIs or GraphQL

Features to Implement:
    Fetch and display data from a public REST API or GraphQL endpoint (e.g., users, posts, products)
    Detail page for a single item
    Create, update, and delete items (CRUD)
    Search and filter data
    Loading and error states
    Responsive design

Tech Stack Suggestions:
    React Router for navigation
    Fetch API, Axios, SWR, or React Query for REST
    Apollo Client for GraphQL
    UI library (Material UI, Chakra UI, Tailwind CSS, or your own styles)

Basic Folder Structure:

src/
  components/
    ItemList.jsx
    ItemDetail.jsx
    ItemForm.jsx
    Navbar.jsx
  api/
    restApi.js         // For REST API calls
    graphqlClient.js   // For GraphQL setup
  App.jsx
  index.js


Key Implementation Steps:
    Set up routing for list and detail pages.
    Create API utility for REST or GraphQL queries/mutations.
    Fetch and display data in components.
    Implement create, update, and delete functionality.
    Add search/filter, loading, and error handling.


Example: Fetching Data from a REST API

import React, { useEffect, useState } from 'react';

function UserList() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetch('https://jsonplaceholder.typicode.com/users')
      .then(res => res.json())
      .then(data => {
        setUsers(data);
        setLoading(false);
      });
  }, []);

  if (loading) return <div>Loading...</div>;
  return (
    <ul>
      {users.map(user => <li key={user.id}>{user.name}</li>)}
    </ul>
  );
}

Example: Fetching Data from a GraphQL API with Apollo Client


import React from 'react';
import { useQuery, gql } from '@apollo/client';

const GET_USERS = gql`
  query {
    users {
      id
      name
    }
  }
`;

function UserList() {
  const { data, loading, error } = useQuery(GET_USERS);

  if (loading) return <div>Loading...</div>;
  if (error) return <div>Error!</div>;
  return (
    <ul>
      {data.users.map(user => <li key={user.id}>{user.name}</li>)}
    </ul>
  );
}